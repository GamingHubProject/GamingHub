{
    "name": "Pelican Connector",
    "class": "App\\Connectors\\PelicanConnector",
    "auth_schema": {
        "application_token": {
            "type": "string",
            "label": "Application API Token",
            "help": "Admin-scoped key — required for server discovery (Application API)."
        },
        "client_token": {
            "type": "string",
            "label": "Client API Token",
            "help": "User-scoped key — required for live resource stats (Client API). Optional if you only need discovery."
        }
    },
    "endpoints": [
        {"id": "server-status", "path": "/api/client/servers/{identifier}/resources", "method": "GET"}
    ],
    "capabilities": ["server-status"]
}
