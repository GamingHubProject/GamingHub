/.phpunit.cache
/.composer-home
/node_modules
/public/build
/resources/spa-dist
/public/hot
/public/storage
/storage/*.key
/storage/pail
/storage/app/geoip
/vendor
.env
.env.backup
.env.production
# Not a secret (build-time-only public config, no credentials) and needed
# in every production build/install — unlike the Laravel .env.production
# convention above, this one is Vite's own, auto-loaded for "vite build".
!spa/.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
