# Production build only (Vite loads this automatically for "vite build",
# never for the dev server). Left empty on purpose: the production build is
# served same-origin from the Laravel app itself (see routes/web.php's /ui
# route + Dockerfile.prod), so API requests should resolve as relative
# URLs against the current origin rather than a separate dev port.
VITE_API_BASE_URL=
